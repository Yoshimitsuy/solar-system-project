import React from 'react';

class SolarSystem extends React.Component {
  render() {
    return (
      <div data-testid="solar-system">só pra não ficar vazio</div>
    );
  }
}

export default SolarSystem;
